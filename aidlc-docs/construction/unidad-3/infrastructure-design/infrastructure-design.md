# Infrastructure Design — Unidad 3: Historial y estadísticas

## Cambios de infraestructura respecto a las Unidades 1-2
**Ninguno.** Esta unidad es puramente de lectura sobre datos ya modelados (`products` con `status = 'bought'`), usando el mismo proyecto Supabase, las mismas políticas RLS de `SELECT`, el mismo hosting Vercel y las mismas variables de entorno.

Todas las categorías evaluadas (Deployment Environment, Compute, Storage, Messaging, Networking, Monitoring, Shared Infrastructure) son **N/A** — no se requiere ningún aprovisionamiento ni configuración adicional.
